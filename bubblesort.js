
const newObj = {};

newObj.bubbleSort = function (arr) {
  // let newArr = []
  // let fakeObj = {
  //   "a" : 1,
  //   "b" : 5,
  //   "c" : 3
  // }
  // const swapValue1 = fakeObj["b"] //5
  // const swapValue2 = fakeObj["c"] //3
  // fakeObj["b"] = swapValue2; //3
  // fakeObj["c"] = swapValue1; //5

  //Base Case
  [1]
  if (arr.length <= 1) {
    return arr
  };


  // for (let i =0; i < arr.length; i++) {
  //   while (arr[i + 1] !== null) {
  //     this.swap(arr[i], arr[i+1])
  //   }

  // }

  return arr

};

// newObj.swap = function (func(a, b) => {

// })


// const bubbleSort = (arr) => {

//   for (let i = 0; i < arr.length; i++) {
//     swap(arr[i], arr[i + 1])
//   }
//   return arr
// };


